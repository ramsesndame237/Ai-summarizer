import asset from '../assets'
const Demo = () => {

    return(
        <section className="w-full mt-16 max-w-xl">
            <div className="flex flex-col w-full gap-2">
                <form onSubmit={() =>{}} className="relative felx justify-center items-center">
                    <img src={asset.linkIcon} alt="" className="absolute"/>
                </form>

            </div>
        </section>
    )
}

export default Demo